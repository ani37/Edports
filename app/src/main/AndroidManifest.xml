<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aniketagarwalla.task1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main" />
        <activity android:name=".BrainBuzz" />
        <activity android:name=".GuessNum" />
        <activity android:name=".ARpuzz" />
        <activity android:name=".math" />
        <activity android:name=".Cal" />
        <activity android:name=".BrainBuzz1" />
        <activity android:name=".math1" />
        <activity android:name=".math3" />
        <activity android:name=".dashboard"></activity>
    </application>

</manifest>